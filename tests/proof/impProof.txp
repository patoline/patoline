\Caml(open ProofTree)

\begin{proofTree}
\Caml(
let p =
  ax     <$ Γ ⊢ t : A⇒B $>;
  ax     <$ Γ ⊢ u : A   $>;
  binary ~name:<$⇒_e$> <$ Γ ⊢ t u : B $>;
  display_proof ()
)
$$ \id(p) $$
\end{proofTree}
