
\Caml(

  let b = <<$bing$>>

  
let _ = newPar D.structure ~environment:(fun x -> x) Complete.normal parameters
  [
    T "On commence par passer en maths et" ;
    B (fun env -> List.map (fun b -> Box.resize env.size b)
      (Maths.draw [ env ] 
      [ Maths.Ordinary (Maths.noad (fun env st -> 
	[Box.Drawing
	    (Box.drawing
	       (Box.draw_boxes (Document.boxify_scoped env b))
	    )]))]))
  ]

)
