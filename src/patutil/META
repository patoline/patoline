name="patutil"
version="1.0"
description="A library with convenient functions to extend the standard library (part of Patoline)"
requires="camomile,rbuffer"
archive(native)="patutil.cmxa"
archive(byte)="patutil.cma"
