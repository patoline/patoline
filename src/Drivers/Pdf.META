package "Pdf" (
archive(byte)="Pdf.cma"
archive(native)="Pdf.cmxa"
requires="Typography"

package "Parser" (
archive(byte)="pdf_parser.cma"
archive(native)="pdf_parser.cmxa"
requires="Typography"
)

)
